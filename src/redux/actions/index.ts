import {setIsLoading, setOriginSizeImage, setCards, setComments} from './photoGridActions';
import {setIsLoadingComment} from './popupActions';
import {setIsOpenPopup} from './mainActions';

export default {
  setIsLoading,
  setOriginSizeImage,
  setCards,
  setComments,
  setIsLoadingComment,
  setIsOpenPopup
}
